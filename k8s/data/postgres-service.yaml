apiVersion: v1
kind: Service
metadata:
  name: saratube-postgres-service
  namespace: saratube-data
spec:
  type: ClusterIP
  selector:
    app: saratube-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432

apiVersion: v1
kind: Service
metadata:
  name: saratube-service
  namespace: saratube-app
spec:
  type: NodePort
  selector:
    app: saratube-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30087
